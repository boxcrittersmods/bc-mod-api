# bc-mod-api

# Routes
* versions
* feedback
* cors
* description

# Data
* textureData
* sitesData

# Libraries
* http
* express
* bc-admin-login
* request (`cors`,`versions`,`description`)
* JSDOM (`versions`,`description`)
* octokit (`feedback`,`version`)
* puppetier (`cors`)
* absolutlify (`cors`)
* imageDataUri (`cors`)
* bodyParser (`feedback`)
* moment (`version`)